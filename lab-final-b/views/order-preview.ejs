<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Preview</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<%- include('partials/header') %>

<div class="container py-5">
  <h1 class="mb-4">Order Preview</h1>

  <h4>Shipping Info</h4>
  <p><strong>Name:</strong> <%= order.customer.fullName %></p>
  <p><strong>Email:</strong> <%= order.customer.email %></p>
  <p><strong>Phone:</strong> <%= order.customer.phone %></p>
  <p><strong>Address:</strong> <%= order.customer.address %>, <%= order.customer.city %>, <%= order.customer.postal %>, <%= order.customer.country %></p>

  <h4 class="mt-4">Items</h4>
  <ul class="list-group mb-3">
    <% order.cart.forEach(item => { %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= item.name %> x <%= item.quantity %>
        <span>Rs.<%= item.price * item.quantity %></span>
      </li>
    <% }) %>
  </ul>

  <div class="mb-3">
    <% if (order.discount) { %>
      <p>Original Total: Rs.<%= order.cart.reduce((sum,i)=>sum+i.price*i.quantity,0) %></p>
      <p>Discount (<%= order.discount.code %>): Rs.<%= order.discount.amount %></p>
      <h4>Total after discount: Rs.<%= total %></h4>
    <% } else { %>
      <h4>Total: Rs.<%= total %></h4>
    <% } %>
  </div>

  <form action="/order/confirm" method="POST">
    <% if (order.discount) { %>
      <input type="hidden" name="couponCode" value="<%= order.discount.code %>">
    <% } %>
    <button type="submit" class="btn btn-success">Confirm Order</button>
  </form>
</div>

<footer class="mt-5">
  <%- include('partials/footer') %>
</footer>
</body>
</html>
