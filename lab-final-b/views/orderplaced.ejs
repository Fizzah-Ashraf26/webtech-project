<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">

  <style>
    body {
      background-color: #f2eee7;
      font-family: Arial, Helvetica, sans-serif;
    }
    .success-box {
      background: white;
      border-radius: 10px;
      padding: 40px;
      text-align: center;
      max-width: 700px;
      margin: 100px auto;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .success-box h1 {
      color: #615450;
      font-weight: normal;
      margin-bottom: 20px;
    }
    .success-box p {
      color: #2b2a29;
      font-size: 18px;
      margin-bottom: 30px;
    }
    .success-box a {
      background: none;
      border: 2px solid #615450;
      color: #615450;
      padding: 10px 20px;
      text-decoration: none;
      transition: 0.3s;
      display: inline-block;
    }
    .success-box a:hover {
      background-color: #332323;
      color: white;
    }
    .order-details {
      text-align: left;
      margin-top: 30px;
      border-top: 1px solid #ccc;
      padding-top: 20px;
    }
    .order-details h5 {
      margin-bottom: 10px;
      color: #615450;
    }
    .order-item {
      display: flex;
      justify-content: space-between;
      padding: 5px 0;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <div class="success-box">
    <i class="fa-solid fa-circle-check fa-3x" style="color: #615450"></i>
    <h1>Order Placed Successfully!</h1>
    <p>
      Thank you, <strong><%= order.customer.fullName %></strong>, for shopping with <strong>Flower</strong>.
      Your bouquet will be delivered soon.
    </p>

    <div class="order-details">
      <h5>Order Summary</h5>
      <p><strong>Order ID:</strong> <%= order._id %></p>
      <p><strong>Total Amount:</strong> Rs.<%= order.totalAmount %></p>
      <p><strong>Payment Method:</strong> <%= order.paymentMethod %></p>

      <h6>Items:</h6>
      <% order.items.forEach(item => { %>
        <div class="order-item">
          <span><%= item.name %> x <%= item.quantity %></span>
          <span>Rs.<%= item.price * item.quantity %></span>
        </div>
      <% }) %>
    </div>

    <a href="/">Back to Home</a>
  </div>

  <footer id="footer" class="mt-5">
    <%- include('partials/footer') %>
  </footer>
</body>
</html>
